pipeline {
    agent any
    stages {
        stage('Feature Build') {
            steps {
                echo "Building feature-login branch"
            }
        }
        stage('Test') {
            steps {
                echo "Running feature-specific tests"
            }
        }
    }
}
